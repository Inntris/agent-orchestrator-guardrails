# Demo Setup Notes

This low-risk documentation file exists so the PASS example can show an AI-generated PR touching an approved path.

Use it when recording the demo:

1. Show the file in the PR diff.
2. Show the Inntris Verification check passing.
3. Open the receipt link to explain why the PR was allowed.

PASS demo note: this change is intentionally limited to documentation and a safe UI component.
